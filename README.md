# ChatbotChambers

Two chatbots talk to each other while the user watches.

## Local setup

1. Copy `.env.example` to `.env`.
2. Fill in any values you need, especially `OPENROUTER_API_KEY`.
3. Run `make dev` to start the app.
